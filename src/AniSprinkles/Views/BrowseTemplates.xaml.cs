namespace AniSprinkles.Views;

/// <summary>
/// Code-behind for the shared browse templates ResourceDictionary — required (with x:Class)
/// for compiled bindings inside the templates. Merged into App.xaml resources.
/// </summary>
public partial class BrowseTemplates : ResourceDictionary
{
    public BrowseTemplates()
    {
        InitializeComponent();
    }
}
